apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: monitoring-access-production
  annotations:
    config.kubernetes.io/local-config: "true"

resources:
  - ../../base

namespace: monitoring

labels:
  - includeSelectors: true
    pairs:
      environment: production
      team: platform
